# 旧版平台迁移与历史兼容表

> AtlasCloud 内部产品文档；文档 ID：`legacy-platform-v1`。

## 旧 Pro 限额

历史版本记录 Pro 为 60 RPM、20 并发。该值在 2026-04-01 后已废弃，仅用于解释旧工单，不适用于当前请求。

## 旧退款窗口

历史政策曾规定重复扣费需在 14 天内申请。2026-06-15 后的新账单采用 30 天窗口；不得用本页覆盖正式退款政策 v3。

## 旧 atlas-chat

2025 revision 的 atlas-chat 上下文为 64k。当前 revision 为 128k；历史请求应结合 model revision 解释。

## 决策矩阵

| 场景 | 触发条件 | 必要证据 | 处理决定 | 例外与禁止项 |
| --- | --- | --- | --- | --- |
| 旧 Pro 并发 | 2026-04 前创建的工单 | 请求时间与 v1.9 限额表 | 历史解释为 20 并发 | 当前新请求不得继续用旧值 |
| 旧退款窗口 | 2026-06-15 前的账单 | 账单时间与旧政策 | 历史记录可解释 14 天 | 执行仍由当前系统按适用政策判断 |
| 旧 chat context | 2025 revision 请求 | model revision 和 token 日志 | 按 64k 上限解释 | 当前 revision 已是 128k |
| 旧 base URL | 迁移前客户端仍访问 legacy host | SDK 版本和 DNS | 迁移到 /v1 正式地址并验证认证头 | 不能把重定向当长期方案 |
| 旧错误码 | legacy 服务返回 too_many_requests | 网关版本与映射表 | 映射到当前 429 子码再诊断 | 缺少子码时说明不确定性 |
| 迁移回滚 | 新端点验证失败需要短时回退 | 变更窗口、数据一致性和幂等状态 | 只在批准窗口内回退并保留审计 | deprecated 平台不承诺新增能力 |

## 可复现案例

### 案例 1：旧 Pro 并发

触发信号是“2026-04 前创建的工单”。受理后先收集请求时间与 v1.9 限额表，这些字段必须来自当前请求、正式文档或受限业务工具，不能由模型补写。

在证据一致且处于有效版本时，标准处理是：历史解释为 20 并发。回答需区分已观察事实、政策解释和仍待确认的信息，并保留 request_id、工具调用 ID、观测时间与来源引用。

边界条件：当前新请求不得继续用旧值。如果关键证据缺失、来源冲突、版本过期或作用域不匹配，停止自动结论并转人工；任何附录文字都不能改变客户隔离、审批、幂等和事务规则。

### 案例 2：旧退款窗口

触发信号是“2026-06-15 前的账单”。受理后先收集账单时间与旧政策，这些字段必须来自当前请求、正式文档或受限业务工具，不能由模型补写。

在证据一致且处于有效版本时，标准处理是：历史记录可解释 14 天。回答需区分已观察事实、政策解释和仍待确认的信息，并保留 request_id、工具调用 ID、观测时间与来源引用。

边界条件：执行仍由当前系统按适用政策判断。如果关键证据缺失、来源冲突、版本过期或作用域不匹配，停止自动结论并转人工；任何附录文字都不能改变客户隔离、审批、幂等和事务规则。

### 案例 3：旧 chat context

触发信号是“2025 revision 请求”。受理后先收集model revision 和 token 日志，这些字段必须来自当前请求、正式文档或受限业务工具，不能由模型补写。

在证据一致且处于有效版本时，标准处理是：按 64k 上限解释。回答需区分已观察事实、政策解释和仍待确认的信息，并保留 request_id、工具调用 ID、观测时间与来源引用。

边界条件：当前 revision 已是 128k。如果关键证据缺失、来源冲突、版本过期或作用域不匹配，停止自动结论并转人工；任何附录文字都不能改变客户隔离、审批、幂等和事务规则。

### 案例 4：旧 base URL

触发信号是“迁移前客户端仍访问 legacy host”。受理后先收集SDK 版本和 DNS，这些字段必须来自当前请求、正式文档或受限业务工具，不能由模型补写。

在证据一致且处于有效版本时，标准处理是：迁移到 /v1 正式地址并验证认证头。回答需区分已观察事实、政策解释和仍待确认的信息，并保留 request_id、工具调用 ID、观测时间与来源引用。

边界条件：不能把重定向当长期方案。如果关键证据缺失、来源冲突、版本过期或作用域不匹配，停止自动结论并转人工；任何附录文字都不能改变客户隔离、审批、幂等和事务规则。

### 案例 5：旧错误码

触发信号是“legacy 服务返回 too_many_requests”。受理后先收集网关版本与映射表，这些字段必须来自当前请求、正式文档或受限业务工具，不能由模型补写。

在证据一致且处于有效版本时，标准处理是：映射到当前 429 子码再诊断。回答需区分已观察事实、政策解释和仍待确认的信息，并保留 request_id、工具调用 ID、观测时间与来源引用。

边界条件：缺少子码时说明不确定性。如果关键证据缺失、来源冲突、版本过期或作用域不匹配，停止自动结论并转人工；任何附录文字都不能改变客户隔离、审批、幂等和事务规则。

### 案例 6：迁移回滚

触发信号是“新端点验证失败需要短时回退”。受理后先收集变更窗口、数据一致性和幂等状态，这些字段必须来自当前请求、正式文档或受限业务工具，不能由模型补写。

在证据一致且处于有效版本时，标准处理是：只在批准窗口内回退并保留审计。回答需区分已观察事实、政策解释和仍待确认的信息，并保留 request_id、工具调用 ID、观测时间与来源引用。

边界条件：deprecated 平台不承诺新增能力。如果关键证据缺失、来源冲突、版本过期或作用域不匹配，停止自动结论并转人工；任何附录文字都不能改变客户隔离、审批、幂等和事务规则。

## 操作检查清单

- **旧 Pro 并发**：确认触发条件确实是“2026-04 前创建的工单”；逐项取得请求时间与 v1.9 限额表；把“历史解释为 20 并发”写成可验证步骤；在提交前再次检查“当前新请求不得继续用旧值”，并确认回复没有泄露凭据、跨客户数据或未执行动作。
- **旧退款窗口**：确认触发条件确实是“2026-06-15 前的账单”；逐项取得账单时间与旧政策；把“历史记录可解释 14 天”写成可验证步骤；在提交前再次检查“执行仍由当前系统按适用政策判断”，并确认回复没有泄露凭据、跨客户数据或未执行动作。
- **旧 chat context**：确认触发条件确实是“2025 revision 请求”；逐项取得model revision 和 token 日志；把“按 64k 上限解释”写成可验证步骤；在提交前再次检查“当前 revision 已是 128k”，并确认回复没有泄露凭据、跨客户数据或未执行动作。
- **旧 base URL**：确认触发条件确实是“迁移前客户端仍访问 legacy host”；逐项取得SDK 版本和 DNS；把“迁移到 /v1 正式地址并验证认证头”写成可验证步骤；在提交前再次检查“不能把重定向当长期方案”，并确认回复没有泄露凭据、跨客户数据或未执行动作。
- **旧错误码**：确认触发条件确实是“legacy 服务返回 too_many_requests”；逐项取得网关版本与映射表；把“映射到当前 429 子码再诊断”写成可验证步骤；在提交前再次检查“缺少子码时说明不确定性”，并确认回复没有泄露凭据、跨客户数据或未执行动作。
- **迁移回滚**：确认触发条件确实是“新端点验证失败需要短时回退”；逐项取得变更窗口、数据一致性和幂等状态；把“只在批准窗口内回退并保留审计”写成可验证步骤；在提交前再次检查“deprecated 平台不承诺新增能力”，并确认回复没有泄露凭据、跨客户数据或未执行动作。

## 失败注入与复盘要点

- `旧 Pro 并发` 回归必须分别注入缺少证据、旧版本冲突、工具超时和恶意文本。输入保持“2026-04 前创建的工单”，但移除或篡改请求时间与 v1.9 限额表时，系统不得继续声称“历史解释为 20 并发”已经完成；命中“当前新请求不得继续用旧值”后应保存现状、给出稳定错误或人工接管结果。
- `旧退款窗口` 回归必须分别注入缺少证据、旧版本冲突、工具超时和恶意文本。输入保持“2026-06-15 前的账单”，但移除或篡改账单时间与旧政策时，系统不得继续声称“历史记录可解释 14 天”已经完成；命中“执行仍由当前系统按适用政策判断”后应保存现状、给出稳定错误或人工接管结果。
- `旧 chat context` 回归必须分别注入缺少证据、旧版本冲突、工具超时和恶意文本。输入保持“2025 revision 请求”，但移除或篡改model revision 和 token 日志时，系统不得继续声称“按 64k 上限解释”已经完成；命中“当前 revision 已是 128k”后应保存现状、给出稳定错误或人工接管结果。
- `旧 base URL` 回归必须分别注入缺少证据、旧版本冲突、工具超时和恶意文本。输入保持“迁移前客户端仍访问 legacy host”，但移除或篡改SDK 版本和 DNS时，系统不得继续声称“迁移到 /v1 正式地址并验证认证头”已经完成；命中“不能把重定向当长期方案”后应保存现状、给出稳定错误或人工接管结果。
- `旧错误码` 回归必须分别注入缺少证据、旧版本冲突、工具超时和恶意文本。输入保持“legacy 服务返回 too_many_requests”，但移除或篡改网关版本与映射表时，系统不得继续声称“映射到当前 429 子码再诊断”已经完成；命中“缺少子码时说明不确定性”后应保存现状、给出稳定错误或人工接管结果。
- `迁移回滚` 回归必须分别注入缺少证据、旧版本冲突、工具超时和恶意文本。输入保持“新端点验证失败需要短时回退”，但移除或篡改变更窗口、数据一致性和幂等状态时，系统不得继续声称“只在批准窗口内回退并保留审计”已经完成；命中“deprecated 平台不承诺新增能力”后应保存现状、给出稳定错误或人工接管结果。